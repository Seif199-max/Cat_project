# 🚀 TaskMaster API (Simple To-Do List)

## 📝 الوصف العام

TaskMaster API هو نظام إدارة مهام بسيط مبني باستخدام **Django** و **Django REST Framework (DRF)**. يتيح للمستخدمين إنشاء، قراءة، تحديث، وحذف المهام (CRUD operations).

تم تصميم هذا المشروع لتوفير واجهة برمجية نظيفة وفعالة لإدارة قائمة مهامك اليومية، مع دعم تحديد الأولويات وتواريخ الاستحقاق.

## 🛠️ التقنيات المستخدمة

| التقنية | الدور |
| :--- | :--- |
| **Python** | لغة البرمجة الأساسية. |
| **Django** | إطار العمل الرئيسي لتطوير الـ Backend. |
| **Django REST Framework (DRF)** | لإنشاء وتوثيق الـ API Endpoints. |
| **Pipenv** | لإدارة البيئة الافتراضية واعتماديات المشروع. |
| **Git/GitHub** | نظام التحكم في الإصدار. |

## ⚙️ الإعداد والتشغيل المحلي (باستخدام Pipenv)

**متطلبات سابقة:** يجب أن يكون لديك Python و Pipenv مثبتان على نظامك.

### 1. استنساخ المستودع (Clone the Repository)

```bash
git clone [URL_مستودع_المشروع]
cd اسم_المشروع
الطريقة (Method),المسار (Path),الوصف,مثال لبيانات الإرسال (Payload Example)
GET,/api/tasks/,استرداد قائمة بجميع المهام.,لا يوجد
POST,/api/tasks/,إنشاء مهمة جديدة.,"{ ""title"": ""Buy groceries"", ""priority"": ""high"", ""category"": 1 }"
GET,/api/tasks/{id}/,استرداد مهمة محددة حسب الـ ID.,لا يوجد
PUT,/api/tasks/{id}/,تحديث جميع حقول مهمة محددة.,"{ ""title"": ""Pay rent"", ""status"": ""completed"" }"
PATCH,/api/tasks/{id}/,تحديث حقل أو أكثر لمهمة محددة.,"{ ""status"": ""in_progress"" }"
DELETE,/api/tasks/{id}/,حذف مهمة محددة.,لا يوجد

---

## 🏆 تحديات التقييم المرفقة (Assessment Challenges)

تم حل التحديات البرمجية وتحديات SQL المطلوبة ضمن هذا المستودع. يمكن العثور على ملفات الحلول في المجلد `/challenges/`.

### 1. تحدي البرمجة (Python)

| الهدف | إزالة العناصر المكررة من قائمة وإرجاع القيم الفريدة. |
| :--- | :--- |
| **الملف في المستودع** | `challenges/python_challenge.py` |

### 2. تحدي SQL (الربط بين الجداول)

| الهدف | استرداد `order_id`، `customer_name`، و `order_total` من جدولي `Customers` و `Orders`. |
| :--- | :--- |
| **الملف في المستودع** | `challenges/sql_challenge.sql` |
| **الاستعلام المُستخدم** | `INNER JOIN` |


---